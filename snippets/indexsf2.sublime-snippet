<snippet>
    <content><![CDATA[
{% extends '::admin_layout.html.twig' %}

{% block actions %}
    <a class="btn primary" href="{{ path('$1_admin_$2_new') }}">{{ '$2.title.new'|trans({}, '$3Bundle') }}</a>
{% endblock %}

{% block page_title %}
    {{ '$2.title.index'|trans({}, '$3Bundle') }}
{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="active">{{ '$2.title.index'|trans({}, 'MSICoreBundle') }}</li>
{% endblock %}

{% block content %}
    {% if $2s|length %}
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for $2 in $2s %}
                    <tr>
                        <td class="span1">{{ $2.id }}</td>
                        <td>
                            {% if $2.published %}
                                <div class="action-change-published bullet green" url="{{ path('$1_admin_$2_change_published', {'id': $2.id}) }}"><span class="hide">1</span></div>
                            {% else %}
                                <div class="action-change-published bullet white" url="{{ path('$1_admin_$2_change_published', {'id': $2.id}) }}"><span class="hide">0</span></div>
                            {% endif %}
                        </td>
                        <td class="span4">
                            <a class="label" href="{{ path('$1_admin_$2_edit', {'id': $2.id}) }}">{{ 'edit'|trans({}, 'MSICoreBundle') }}</a>
                            <a class="action-delete label important" url="{{ path('$1_admin_$2_delete', {'id': $2.id}) }}" href="#">{{ 'delete'|trans({}, 'MSICoreBundle') }}</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <h2>{{ 'not.found'|trans({}, 'MSICoreBundle') }}</h2>
    {% endif %}
{% endblock %}
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>indexsf2</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <!-- <scope>source.python</scope> -->
</snippet>
